const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' //operador ternário é composto por três partes: uma expressão (verdadeiro ou falso), a '?' e informa a mensagem quando for verdadeiro, e ':' e informa a mensagem quando for falso.

console.log(resultado(7.1))
console.log(resultado(6.7))
